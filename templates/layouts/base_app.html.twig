<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{% block title %}TicketApp{% endblock %}</title>
    <link rel="stylesheet" href="/styles/index.css">
  </head>
  <body>
    <header class="site-header">
      <div class="container nav">
        <div><strong>TicketApp</strong></div>
        <div class="nav-links">
          <a href="/?page=tickets">Tickets</a>
          <form method="post" action="/?page=auth/logout" style="display:inline">
            <button class="btn-ghost" type="submit">Logout</button>
          </form>
        </div>
      </div>
    </header>

    <main>
      {% block body %}{% endblock %}
    </main>

    <footer class="site-footer">
      <div class="container">© {{ "now"|date("Y") }} TicketApp</div>
    </footer>

    {% if flash %}
      {% include 'components/toast.html.twig' with { flash: flash } %}
    {% endif %}
  </body>
</html>
